# Define the project key and name
sonar.projectKey=my-website
sonar.projectName=My Website

# Define the source directory (adjust this to your project structure)
sonar.sources=backend,frontend

# You can also exclude specific files if necessary
# sonar.exclusions=**/*.test.js,**/node_modules/**

# Set the language if necessary (SonarQube can auto-detect)
sonar.language=js
